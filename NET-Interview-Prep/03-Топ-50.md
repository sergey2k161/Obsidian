# Топ-50 вопросов для собеседования

> Ключевые вопросы, которые чаще всего встречаются на собеседованиях

## .NET Framework и типы данных

### 1. Что такое .NET framework и чем отличается .NET Core?

**.NET Framework** — платформа для Windows-приложений (WinForms, WPF, ASP.NET).
**.NET Core** — кроссплатформенная, модульная, с открытым исходным кодом, работает на Windows, Linux, macOS.

### 2. Может ли значение типа DateTime принимать значение null?

Нет, `DateTime` — **Value Type**, не может быть `null`.  
Чтобы мог — использовать `Nullable<DateTime>` или `DateTime?`.


### 3. Перечисления (Enum) это Value type или Reference type?

**Value type**, так как наследуется от `System.Enum`, который базируется на `System.ValueType`.


### 4. Ваше понимание работы класса StringBuilder. Есть ли преимущество перед использованием String?

Используется для **эффективного изменения строк** (конкатенации).  
`String` неизменяем (immutable), при каждой операции создаётся новый объект.  
➡️ `StringBuilder` быстрее при множественных изменениях текста.

## Модификаторы

### 5. Модификаторы доступа

- `private` — доступ внутри класса
    
- `protected` — внутри класса и наследников
    
- `internal` — внутри сборки
    
- `protected internal` — внутри сборки и наследников
    
- `public` — везде


### 6. Модификатор virtual, abstract

- `virtual` — метод имеет реализацию, но может быть переопределён
    
- `abstract` — нет реализации, обязан быть переопределён в наследнике  
    (используется только в `abstract class`)


### 7. Модификатор new

Скрывает (shadow) член базового класса с тем же именем.  
Не переопределяет, а именно **скрывает**.

### 8. Модификатор static

Принадлежит **классу**, а не объекту.  
Общие данные и методы, не требуют создания экземпляра.


### 9. Ваше понимание Extension-методов. Как объявить метод расширения?

Позволяют **добавить метод к существующему типу** без его изменения.

```csharp
public static class MyExtensions 
{     
	public static bool IsEven(this int num) => num % 2 == 0; 
}
```

## Делегаты, события и лямбда-выражения

### 10. Ваше понимание Delegate и Multicast Delegate.

`Delegate` — ссылка на метод (или несколько).  
`MulticastDelegate` — может хранить несколько методов (используется через `+=`).

### 11. Ваше понимание Event

Обёртка над делегатом, **инкапсулирует вызов**: подписка/отписка (`+=`, `-=`),  
но вызывать могут только внутри класса-источника.


### 12. Ваше понимание анонимного метода, lambda-выражений

Способ `кратко` записать выражение,  используется в `LINQ`


## ООП

### 13. В чем отличие абстрактного класса от интерфейса?

`Абстрактный класс` –  это класс, экземпляр которого мы не можем создать, в нем есть и реализованные методы и не реализованные.

Интерфейс не предполагает реализацию.

Абстрактный класс используется для построения архитектуры наследования, в то время как интерфейс, показывает что должен уметь делать тот или иной класс.


### 14. Опишите различие между readonly полями и константами.

- `const` — одно фиксированное значение для всех и навсегда.
    
- `readonly` — значение может быть разным для разных объектов, но остаётся неизменным после присвоения.

## Приведение типов

### 15. Ваше понимание инструкций is и as

****`is`** — проверяет, можно ли преобразовать объект к указанному типу (возвращает `bool`):
**`as`** — пытается привести объект к типу, **не выбрасывая исключение**.  
Возвращает `null`, если преобразование невозможно:

## Generics (Обобщенные типы)

### 16. Ваше понимание обобщенных (generic) типов

**Обобщённые типы (Generics)** — это механизмы, позволяющие создавать **классы, методы и интерфейсы**,  
которые работают с **разными типами данных**, сохраняя **типобезопасность** и **производительность**.

📘 Пример:

```csharp
List<int> numbers = new List<int>(); 
List<string> names = new List<string>();
```

### 17. Ваше понимание ключевого слова default

`default` возвращает **значение по умолчанию** для указанного типа `T`.
```csharp
int i = default;          // 0
string s = default;       // null
bool b = default;         // false

T value = default(T);     // универсальный вариант

```

### 18. Ваше понимание ограничений параметров обобщенных типов

```csharp
where T : class           // только ссылочный тип
where T : struct          // только значимый тип
where T : new()           // нужен конструктор без параметров
where T : BaseClass       // наследование
where T : IInterface       // реализация интерфейса
```


## Коллекции

### 19. Ваше понимание IEnumerable, IEnumerator?

- `IEnumerable` — предоставляет `GetEnumerator()`
    
- `IEnumerator` — умеет `MoveNext()`, `Current`, `Reset()`.  
    Используются в `foreach`.


### 20. В чем разница между IDictionary.Add(key, value) и IDictionary[key] = value

- `Add` — выбросит ошибку, если ключ существует
    
- `[]=` — заменит значение по ключу, если есть

## Обработка исключений

### 21. Можно ли написать несколько блоков catch для одного оператора try?

Да


### 22. Есть ли отличие в catch(Exception e){throw e;} и catch(Exception e){throw;}?

- `throw e` — **сбрасывает стек вызовов**
    
- `throw` — сохраняет оригинальный стек (использовать предпочтительно)


## Параметры методов

### 23. Ваше понимание модификаторов ref и out

Позволяют передать аргументы в метод по ссылке.  Это означает, что изменения, внесенные в аргументы внутри метода, сохраняются и после выхода из метода.
![[Pasted image 20251110145455.png]]

### 24. Ваше понимание ключевого слова params

Позволяет передавать **переменное количество аргументов**:


## Управление памятью

### 25. Как происходит сборка мусора в .NET?

В C# куча делится на управляемая и не управляемая, управляемая, это та, с которой сборщик мусора может работать. При работе с неуправляемыми ресурсами, нужно все обернуть в using, который содержит блок try/finally в котором вызывается метом Dispose.

**Finalize** – его мы на прямую не вызываем, это делает сборщик мусора. Строится граф связей, если есть объекты, к которым нет уже ссылки, то такие объекты помещаются в очередь финализации и при следующей сборке мусора они очиститься.

**ПОКОЛЕНИЯ**

Куча бывает 3 видов **Small Object Heap (****до** **85кб****), Large Object Heap(более** **85кб****) и** **Pinned Object Heap.** Есть 3 поколения «0, 1, 2», при каждой сборке если объект не очищен то его поколение увеличивается, чем выше поколение, тем меньше шанс его удалить, после очистки, память дефрагментированна и все объекты выстраиваются в 1 линию, чтобы проще было выделить память под новые объекты. Если объектов 1 поколения слишком мало, то сборщик мусора может пропустить анализ. Когда добавляются большие объекты, то они сразу становятся 2 поколением, после сборки мусора они не перемещаются.


### 26. В чем отличие структур от классов

|Структуры (struct)|Классы (class)|
|---|---|
|Value type|Reference type|
|В стеке|В куче|
|Не поддерживают наследование|Поддерживают|
|Нет конструктора по умолчанию с параметрами|Есть|


## LINQ и запросы

### 27. Ваше понимание IQueryable. В чем ключевое отличие от IEnumerable?

Тут стоит рассказать про разницу **IEnumerable** и **IQueryble.**

Различия в том, на каком из уровней происходит фильтрация наших данных. Если мы используем **IQueryble,** то фильтрация происходит на уровне базы данных, что является более оптимальным


## Многопоточность

### 28. Объясните разницу между нитью (Thread), задачей (Task) и процессом (Process)

- **Process** — выполняемая программа
    
- **Thread** — поток внутри процесса
    
- **Task** — надстройка над потоком, управляемая планировщиком (async/await)


### 29. Ваше понимание double-check-locking, Синхронизация потоков

Паттерн для потокобезопасной инициализации:

## ORM и архитектура

### 30. Ваше понимание ORM. Какими вы пользовались?

ORM (Object-Relational Mapping) — связывает объекты C# с таблицами БД.
*Entity Framework

### 31. Ваше понимание принципа SOLID

**S — Single Responsibility Principle (Принцип единственной ответственности)** - Каждый класс должен иметь только одну зону ответственности.

**O — Open/Closed Principle -** (Открыт для расширения, закрыт для изменения).  
![[Pasted image 20251110150006.png]]

**L — Liskov Substitution Principle (Принцип подстановки Барбары Лисков) -**  
Объекты подклассов должны заменять объекты суперклассов без ошибок.
![[Pasted image 20251110150015.png]]


**I —** **Interface** **Segregation** **Principle (Принцип разделения интерфейса) -  
**Лучше много маленьких интерфейсов, чем один жирный.
![[Pasted image 20251110150031.png]]


**D —** **Dependency** **Inversion** **Principle (Принцип инверсии зависимостей) -  
**Зависеть нужно от абстракций, а не от конкретных классов.
![[Pasted image 20251110150127.png]]



### 32. Паттерны проектирования (Singleton, Abstract Factory, Builder)

- **Singleton** — один экземпляр класса
    
- **Abstract Factory** — создание семейств объектов
    
- **Builder** — пошаговое построение сложного объекта


## Асинхронность

### 33. Конструкции async await
C# асинхронность реализуется с помощью ключевых слов **async** и **await**. Это позволяет не блокировать поток выполнения, например при обращении к БД. Асинхронный метод возвращаетTask или TaskT, и его можно await-ить, чтобы продолжить выполнение после завершения задачи.
**Связано с:** [[Middle#33. Какую задачу решает модификатор async?]], [[Middle#34. Для чего нужен оператор await?]]


### 34. Что такое deadlock?


---

# SQL

## Операторы

### 35. Операторы DDL, DML

**Связано с:** [[#76. Операторы DDL]], [[#77. Операторы DML]]


### 36. Виды JOIN

**Связано с:** [[#79. Виды JOIN]]


### 37. Что такое HAVING и чем отличается от GROUP BY

**Связано с:** [[#80. Что такое HAVING и чем отличается от GROUP BY]]


## Индексы и производительность

### 38. Что такое индекс и какие виды индексов есть в БД продукта?

**Связано с:** [[#81. Что такое индекс и какие виды индексов есть в БД продукта?]]


### 39. Как посмотреть список выполняющихся запросов в БД продукта?

**Связано с:** [[#82. Как посмотреть список выполняющихся запросов в БД продукта?]]


### 40. Что такое план запроса и как его читать?

**Связано с:** [[#83. Что такое план запроса и как его читать?]]


## Объекты БД

### 41. Временные таблицы и представления

**Связано с:** [[Middle#85. Временные таблицы и представления]]


### 42. Триггеры и хранимые процедуры

**Связано с:** [[Middle#93. Триггеры]], [[Middle#92. Логика в БД, хранимые процедуры]]


---

## Статистика подготовки

**Всего вопросов:** 42

**Изучено:** 0/42

**Прогресс:**
- [ ] .NET Framework и типы (1-4) - 0/4
- [ ] Модификаторы (5-9) - 0/5
- [ ] Делегаты и события (10-12) - 0/3
- [ ] ООП (13-14) - 0/2
- [ ] Типы и Generics (15-18) - 0/4
- [ ] Коллекции (19-20) - 0/2
- [ ] Исключения (21-22) - 0/2
- [ ] Параметры (23-24) - 0/2
- [ ] Память (25-26) - 0/2
- [ ] LINQ (27) - 0/1
- [ ] Многопоточность (28-29) - 0/2
- [ ] Архитектура (30-32) - 0/3
- [ ] Async (33-34) - 0/2
- [ ] SQL Операторы (35-37) - 0/3
- [ ] SQL Производительность (38-40) - 0/3
- [ ] SQL Объекты (41-42) - 0/2

---

## Быстрый доступ

### Критически важные темы
- [[#25. Как происходит сборка мусора в .NET?]]
- [[#28. Объясните разницу между нитью (Thread), задачей (Task) и процессом (Process)]]
- [[#31. Ваше понимание принципа SOLID]]
- [[#33. Конструкции async await]]

### Часто спрашиваемые на собеседованиях
- [[#4. Ваше понимание работы класса StringBuilder. Есть ли преимущество перед использованием String?]]
- [[#13. В чем отличие абстрактного класса от интерфейса?]]
- [[#22. Есть ли отличие в catch(Exception e){throw e;} и catch(Exception e){throw;}?]]
- [[#27. Ваше понимание IQueryable. В чем ключевое отличие от IEnumerable?]]