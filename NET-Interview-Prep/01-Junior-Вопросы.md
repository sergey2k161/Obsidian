# C# и .NET - Вопросы для подготовки

## .NET Framework

### 1. Что такое .NET framework?

**.NET Framework** — это программная платформа, разработанная компанией **Microsoft** для создания, выполнения и управления приложениями, написанными на различных языках программирования (C#, VB.NET, F# и др.).  
Она обеспечивает:

- **Единую среду выполнения** для всех .NET-языков;
    
- **Большую библиотеку классов (FCL)** для типичных задач (работа с файлами, БД, сетью и т. д.);
    
- **Управляемое выполнение кода** (сборка мусора, безопасность, типобезопасность).
### 2. Опишите CLR

**CLR** — это **общеязыковая среда выполнения**, основной компонент .NET Framework.  
Она отвечает за выполнение управляемого кода и предоставляет такие функции, как:

- Загрузка и выполнение сборок;
    
- Управление памятью (в том числе **Garbage Collector**);
    
- Безопасность кода;
    
- Обработка исключений;
    
- Межъязыковое взаимодействие (все .NET-языки работают через CLR ).
    

Проще говоря, CLR — это “виртуальная машина” для .NET-приложений, аналог **JVM** в мире Java.
### 3. Опишите FCL

**FCL** — это **большая библиотека классов**, входящая в состав .NET Framework.  
Она предоставляет готовые типы и классы для решения стандартных задач:

- работа с файлами, потоками, коллекциями;
    
- доступ к БД (ADO.NET);
    
- сетевое взаимодействие;
    
- создание графического интерфейса (Windows Forms, WPF);
    
- сериализация, LINQ, XML и др.
    

FCL — это основа для быстрой разработки приложений на .NET.
### 4. Опишите CLS

**CLS** — это **набор правил и ограничений**, определяющих, как языки .NET должны взаимодействовать между собой.  
Она описывает минимальные требования, которым должен соответствовать язык, чтобы код, написанный на нём, был **совместим с другими .NET-языками**.

Например, если класс соответствует CLS, то им можно пользоваться из любого .NET-языка — C#, VB.NET, F# и т.д.
### 5. Опишите CIL
**CIL** (ранее назывался **MSIL — Microsoft Intermediate Language**) — это **промежуточный язык**, в который компилируется код .NET-языков.  
Файлы на C#, VB.NET и т.п. сначала компилируются в **CIL-код**, а уже при запуске приложения CLR с помощью **JIT-компилятора** (Just-In-Time) превращает CIL в машинный код конкретной платформы.

Таким образом, CIL обеспечивает **независимость кода от платформы и языка**.

## Сборки

### 6. Что такое сборка (Assembly)?


### 7. Что такое GAC? Какую проблему он разрешает?


### 8. Что такое частные и общие сборки?


## Типы данных

### 9. Какие методы класса Object вы знаете?

Все типы в .NET неявно наследуются от базового класса **`System.Object`**, поэтому имеют его методы:

| Метод                | Назначение                                                                   |
| -------------------- | ---------------------------------------------------------------------------- |
| `Equals(object obj)` | Сравнивает объекты на равенство.                                             |
| `GetHashCode()`      | Возвращает хэш-код объекта.                                                  |
| `ToString()`         | Возвращает строковое представление объекта.                                  |
| `GetType()`          | Возвращает тип объекта (`System.Type`).                                      |
| `MemberwiseClone()`  | Создаёт поверхностную копию объекта.                                         |
| `Finalize()`         | Вызывается перед уничтожением объекта сборщиком мусора (аналог деструктора). |
### 10. Что такое Value Type и Reference Type? Назовите различия между ними.
| Характеристика         | **Value Type (значимые типы)**            | **Reference Type (ссылочные типы)**                            |
| ---------------------- | ----------------------------------------- | -------------------------------------------------------------- |
| **Хранение**           | Хранят значение **в стеке**               | Хранят **ссылку в стеке**, а само значение — **в куче (heap)** |
| **Примеры**            | `int`, `double`, `bool`, `struct`, `enum` | `class`, `interface`, `string`, `array`, `delegate`            |
| **Передача в метод**   | Передаётся **копия значения**             | Передаётся **ссылка на объект**                                |
| **Может ли быть null** | Нет (только `Nullable<T>`)                | Да                                                             |
| **Создание**           | Создаётся без оператора `new` (обычно)    | Требует `new` (кроме `string` литералов)                       |

### 11. Ваше понимание упаковки (Boxing) и распаковки (Unboxing). Приведите пример.

**Boxing (упаковка)** — процесс преобразования **значимого типа** в **объект (object)** или другой ссылочный тип.  
**Unboxing (распаковка)** — обратный процесс: преобразование объекта обратно в значимый тип.

📘 Пример:
```csharp
int value = 10;         // Value Type 
object obj = value;      // Boxing 
int newValue = (int)obj; // Unboxing`
```
🧠 **Важно:** упаковка создает **новый объект в куче**, что может снижать производительность при частом использовании (например, при работе с коллекциями без Generics).
### 12. Может ли значение типа DateTime принимать значение null?

Нет, **`DateTime` — это Value Type (структура)**, и по умолчанию не может быть `null`.

✅ Но можно использовать **nullable-тип**:

```csharp
DateTime? date = null; 
`if (date.HasValue) Console.WriteLine(date.Value);
```
Таким образом, `DateTime?` — это сокращение для `Nullable<DateTime>`.
### 13. Перечисления (Enum) это Value type или Reference type?

**Enum (перечисления)** — это **Value Type**.  
Они **наследуются от `System.Enum`**, который, в свою очередь, наследуется от `System.ValueType`.
### 14. Какие типы могут быть использованы в качестве значений перечисления?

**Любые целочисленные** — исключение **char**.  
## Строки

### 15. Строки (String) это Value type или Reference type?

`String` — это **reference type (ссылочный тип)**.
### 16. Ваше понимание immutable.

**Immutable (неизменяемый)** — это объект, **состояние которого нельзя изменить после создания**.
### 17. Что происходит в процессе конкатенации строк?

Когда вы выполняете конкатенацию (`+`, `+=`, `String.Concat`, `String.Format` и т.д.),  
на самом деле **создаётся новый объект `string`** в памяти, а старые — остаются неизменными.
### 18. Формирование строки из массива

Дан массив строк:
```csharp
var strArray = new[]{"Str1","Str2","Str3"……"Str1000"}
```

Какими механизмами вы воспользуетесь для формирования строки, содержащей все элементы массива с разделителями? Почему именно эти механизмы?

---
#### 🔹 **Лучший способ:**

```csharp
string result = string.Join(", ", strArray);
```

📘 **Почему именно `string.Join`:**

- Работает **на уровне системных оптимизаций** (использует внутренний `StringBuilder`);
    
- Не создаёт множество промежуточных строк;
    
- Быстрее и экономичнее, чем конкатенация в цикле.
---
#### 🔹 **Альтернатива:**

Если требуется более сложная логика форматирования — используйте **`StringBuilder`**:

```csharp
var sb = new StringBuilder(); foreach (var s in strArray) {     sb.Append(s).Append(", "); } string result = sb.ToString().TrimEnd(',', ' ');
```

🧠 **Когда что выбирать:**

- ✅ `string.Join` — для простого объединения с разделителем;
    
- ✅ `StringBuilder` — если нужно условное добавление, сложное форматирование или огромные объёмы данных.
### 19. Ваше понимание работы класса StringBuilder. Есть ли преимущество перед использованием String?

**`StringBuilder`** — это класс для эффективного построения и изменения строк.  
В отличие от `string`, он **изменяемый (mutable)**, т.е. при добавлении текста **не создаёт новый объект**.
## Модификаторы доступа и ключевые слова

### 20. Модификатор abstract

`abstract` обозначает, что:

- Класс **не может быть создан напрямую** (нельзя `new`);
    
- Метод **должен быть переопределён** в наследнике.
📘 Пример:
```csharp
public abstract class Animal {     
	public abstract void Speak(); 
}
```

### 21. В каком случае вы обязаны использовать модификатор abstract?

Если вы хотите, чтобы:

- Класс **служил базой**, но не создавался напрямую;
    
- Метод **не имел реализации**, а требовал обязательного переопределения в наследниках.
### 22. Модификатор sealed

`sealed` запрещает **наследование** от класса.

📘 Пример:
```csharp
public sealed class Logger {}`
```
Также можно **запечатать метод**, чтобы запретить дальнейшее переопределение:
```csharp
public sealed override void Print() {}
```
### 23. Может ли класс одновременно иметь модификаторы abstract и sealed?

Нет ❌

- `abstract` требует **наследования**;
    
- `sealed` — **запрещает наследование**.  
    Они **взаимоисключающие**.
### 24. Модификатор virtual

Позволяет **переопределять метод** в классах-наследниках с помощью `override`.
### 25. В чем разница между абстрактными и виртуальными методами?

| Свойство                    | **abstract**                                    | **virtual**                  |
| --------------------------- | ----------------------------------------------- | ---------------------------- |
| Реализация в базовом классе | Нет                                             | Есть                         |
| Обязательно переопределять? | Да                                              | Нет                          |
| Может вызываться напрямую?  | Нет (нельзя создать объект абстрактного класса) | Да                           |
| Использование               | Определяет «контракт»                           | Позволяет изменить поведение |
### 26. Модификатор protected

Позволяет доступ **только из класса и его наследников**.
### 27. Модификатор internal

Доступен **в пределах одной сборки (assembly)**, но недоступен извне.
### 28. Модификатор private

Ограничивает доступ **только внутри текущего класса**.  
Самый строгий уровень инкапсуляции.
### 29. Наследуются ли члены класса с модификатором private?

Нет, **private-члены не наследуются**.  
Они видны только внутри того класса, где объявлены.
### 30. Модификатор new

Скрывает элемент базового класса с таким же именем.  
Используется для **явного сокрытия** унаследованных членов.

📘 Пример:

```csharp
class Base { 
	public void Show() => Console.WriteLine("Base"); 
} 

class Child : Base {     
	public new void Show() => Console.WriteLine("Child"); 
}
```
### 31. Модификатор protected internal

Доступен:

- Из **наследников** (даже из других сборок);
    
- И из **текущей сборки**.
### 32. Модификатор static

Применяется к **членам или классам**, не зависящим от экземпляра.

📘 Особенности:

- Статические поля и методы принадлежат **классу**, а не объекту;
    
- Доступ через имя класса (`ClassName.Method()`);
    
- Не допускает ключевого слова `this`.
### 33. Ваше понимание статического класса

Класс, который:

- Не может быть создан (`new`);
    
- Содержит **только статические члены**;
    
- Часто используется как **утилитарный** (вспомогательный) класс.
### 34. Ваше понимание статического метода

Метод, принадлежащий **классу**, а не объекту.  
Его можно вызывать **без создания экземпляра**.
### 35. Ваше понимание Extension-методов. Как объявить метод расширения?

**Extension-методы (методы расширения)** — позволяют **добавлять новые методы** к существующим типам **без изменения их кода**.
📘 Объявляются:
- В **статическом классе**;
    
- Сам метод — **статический**;
    
- Первый параметр имеет модификатор `this` и указывает, к какому типу применяется метод.
## Делегаты и события

### 36. Ваше понимание Delegate и Multicast Delegate.

**Delegate (делегат)** — это **тип, который хранит ссылку на метод** (или несколько методов).  
По сути — это «указатель на метод» в C#.  
Он позволяет вызывать метод через переменную.
**Multicast Delegate** — делегат, который может содержать **несколько методов в списке вызова**.  
Добавляются через оператор `+=`.
### 37. Может ли Delegate принимать значение null?

Да ✅  
Делегат — это **ссылочный тип**, поэтому может быть `null`.
```csharp
d?.Invoke("Hello");
```
### 38. Ваше понимание Event

**Event (событие)** — это механизм **уведомления о событии**.  
Это **инкапсулированный делегат**, который позволяет подписчикам получать уведомления,  
но не позволяет внешнему коду напрямую вызвать делегат (только через `Invoke` внутри класса).

### 39. Может ли Event принимать значение null?

Да ✅  
Если на событие **нет подписчиков**, оно равно `null`.  
Поэтому перед вызовом обычно делают проверку:

`Clicked?.Invoke(); // безопасный вызов`
### 40. Ваше понимание анонимного метода, lambda-выражений

**Анонимный метод** — это метод **без имени**, объявленный прямо в месте использования делегата.  
**Lambda-выражение** — короткая запись анонимного метода.

```csharp
// Анонимный метод
MyDelegate d = delegate(string s) { Console.WriteLine(s); };

// Лямбда-выражение
MyDelegate d2 = s => Console.WriteLine(s);
```
🧠 Лямбды — синтаксический сахар над анонимными методами,  
и активно используются в LINQ и событиях.
## ООП: Классы и интерфейсы

### 41. Ваше понимание абстрактного класса

Абстрактный класс:

- Не может быть создан напрямую (`new` запрещён);
    
- Может содержать как **реализованные**, так и **абстрактные методы**;
    
- Используется как **базовый** для других классов.
 ```csharp
 public abstract class Shape {
    public abstract double Area();
}  
```
### 42. Чем перекрытый метод отличается от перегруженного метода?

|Тип|Ключевое слово|Суть|
|---|---|---|
|**Перекрытие (override)**|`override`|Замещает метод базового класса|
|**Перегрузка (overload)**|Одинаковое имя, разные параметры|Разные сигнатуры методов|
### 43. Ваше понимание интерфейса

**Интерфейс (`interface`)** — это **контракт**, который определяет набор методов, свойств, событий и индексов **без реализации**.  
Класс, реализующий интерфейс, обязан реализовать все его члены.
### 44. В чем отличие абстрактного класса от интерфейса?

| Отличие             | **Абстрактный класс** | **Интерфейс**                                       |
| ------------------- | --------------------- | --------------------------------------------------- |
| Содержит реализацию | Может содержать       | Не содержал (до C# 8, где появились default-методы) |
| Поля и конструкторы | Может иметь           | Нет                                                 |
| Наследование        | Один базовый класс    | Несколько интерфейсов                               |
| Используется когда  | Есть общая логика     | Только контракт                                     |
### 45. Может ли интерфейс содержать делегаты?

Да ✅  
Интерфейс может содержать **объявление делегата**, как типа.

📘 Пример:

```csharp
public interface IProcessor {     
	public delegate void ProcessHandler(string data); 
}
```
### 46. Может ли интерфейс содержать события (Event)?
Да ✅  
Интерфейс может объявлять события (без реализации).

📘 Пример:
```csharp
public interface INotifier {
    event Action OnNotify;
}
```
### 47. Возможно ли множественное наследование интерфейсов в C#?

Да ✅  
Класс или интерфейс может реализовывать **несколько интерфейсов**.
### 48. Возможно ли множественное наследование классов в C#?

Нет ❌  
C# поддерживает **только одиночное наследование классов**,  
но допускает **множественную реализацию интерфейсов**.
### 49. Возможно ли разрешить наследование класса, но запретить перекрытие метода?

Да ✅  
Для этого метод объявляется **без `virtual`** или помечается как **`sealed override`**.
```csharp
public class A {
    public void Print() {} // нельзя переопределить
}

public class B : A {
    public sealed override void ToString() {} // можно запретить дальше
}
```
### 50. Реализация интерфейсов с одинаковыми методами
Даны два интерфейса:
```csharp
public interface IWorkerA { int DoWork(); }
public interface IWorkerB { int DoWork(); }
```
Каким образом реализовать оба интерфейса в одном классе?
```csharp
public class Worker: IWorkerA, IWorkerB {}
```

---
Когда два интерфейса имеют методы с одинаковыми сигнатурами,  
можно реализовать их **явно** (explicit interface implementation):

📘 Пример:
```csharp
public class Worker : IWorkerA, IWorkerB 
{     
	int IWorkerA.DoWork() => 1;     
	int IWorkerB.DoWork() => 2; 
}
```

## Константы и поля

### 51. Опишите различие между readonly полями и константами.
- `const` — одно фиксированное значение для всех и навсегда.
    
- `readonly` — значение может быть разным для разных объектов, но остаётся неизменным после присвоения.

**ДУШНО:

| Свойство                   | **const**                | **readonly**                      |
| -------------------------- | ------------------------ | --------------------------------- |
| Присвоение                 | Только при объявлении    | При объявлении или в конструкторе |
| Тип                        | Только примитивы/строки  | Любой тип                         |
| Изменение после компиляции | Нет (встраивается в код) | Можно через конструктор           |
| Время вычисления           | Компиляция               | Выполнение                        |
## Приведение типов

### 52. Ваше понимание инструкций is и as

**`is`** — проверяет, можно ли преобразовать объект к указанному типу (возвращает `bool`):
**`as`** — пытается привести объект к типу, **не выбрасывая исключение**.  
Возвращает `null`, если преобразование невозможно:
### 53. Поведение операторов приведения типов

Дан следующий код:
```csharp
var value = 1;
1: var result = (TType)value;
2: var result = (value as TType)
```

**Вопросы:**
- Что произойдет если в выражении `(TType)value`, значение value не приводимо к типу TType?
-❌ Выбрасывается `InvalidCastException`

- Что произойдет если в выражении `(value as TType)`, значение value не приводимо к типу TType?
-✅ Возвращает `null` (без исключения)

- Что произойдет если в выражении `(value as TType)`, значение value является Value Type?
-❌ Ошибка компиляции — `as` работает только с ссылочными типами и `Nullable<T>`

- Что произойдет если в выражении `(value as TType)`, значение value является Reference Type, не приводимым к TType? 
-✅ Вернёт `null`


## Обобщенные типы (Generics)

### 54. Ваше понимание обобщенных (generic) типов

**Обобщённые типы (Generics)** — это механизмы, позволяющие создавать **классы, методы и интерфейсы**,  
которые работают с **разными типами данных**, сохраняя **типобезопасность** и **производительность**.

📘 Пример:

```csharp
List<int> numbers = new List<int>(); 
List<string> names = new List<string>();`
```

### 55. Какую проблему решали обобщённые коллекции?

Они **типобезопасны**, **эффективны** и **удобны**.
### 56. Ваше понимание ключевого слова default

`default` возвращает **значение по умолчанию** для указанного типа `T`.
```csharp
int i = default;          // 0
string s = default;       // null
bool b = default;         // false

T value = default(T);     // универсальный вариант

```
### 57. Ваше понимание ограничений параметров обобщенных типов
## ⚖️ Зачем нужны ограничения

- Чтобы компилятор **знал**, какие операции допустимы с типом `T`.
    
- Чтобы избежать ошибок во время выполнения.
    
- Чтобы писать более гибкий и безопасный код.

👉 Простыми словами: **Ограничения позволяют сказать компилятору: "Тип** `T` **должен обладать определёнными свойствами (быть классом, структурой, наследником, иметь конструктор и т.д.)".**
## Коллекции и итераторы

### 58. Что можно использовать в качестве аргумента в конструкции foreach?

Любой объект, который реализует **интерфейс `IEnumerable`** или **`IEnumerable<T>`**,  
или имеет метод `GetEnumerator()` с `MoveNext()` и `Current`.

📘 Примеры:

- Массивы (`int[]`, `string[]`);
    
- Коллекции (`List<T>`, `Dictionary<TKey,TValue>`, `HashSet<T>`);
    
- Собственные классы, реализующие `IEnumerable`.
### 59. Ваше понимание IEnumerable, IEnumerator?

**Коротко:**

- `IEnumerable` — это интерфейс, который говорит: "Меня можно перебирать". Он предоставляет метод `GetEnumerator()`.
    
- `IEnumerator` — это интерфейс, который отвечает за сам процесс перебора: "Как именно переходить от элемента к элементу".

## 🔹IEnumerable
Определяет коллекцию, которую можно пройти в цикле `foreach`.
Содержит метод:
```csharp
IEnumerator GetEnumerator();
```
## 🔹 IEnumerator

- Управляет **текущей позицией** в коллекции.
```csharp
bool MoveNext();   // перейти к следующему элементу
object Current { get; } // текущий элемент
void Reset();      // вернуться в начало
```
### 60. Ваше понимание Array

`System.Array` — базовый класс для всех массивов в .NET.  
Массив — это **фиксированная по размеру, типобезопасная коллекция элементов** одного типа.
```csharp
int[] nums = new int[3] { 1, 2, 3 };
```
### 61. Объясните разницу между System.Array.CopyTo() и System.Array.Clone()

|Метод|Описание|
|---|---|
|`CopyTo()`|Копирует элементы **в другой существующий массив**, начиная с заданного индекса|
|`Clone()`|Создаёт **поверхностную копию** массива и возвращает **новый объект**|
### 62. Ваше понимание коллекций и списков

**Коллекции** — это обобщённые структуры данных для хранения элементов (`List`, `Dictionary`, `Queue`, `Stack` и др.).

**Списки (`List<T>`)** — динамические массивы,  
которые автоматически увеличивают размер при добавлении элементов.
Таким образом, список — это частный случай коллекции. Все списки являются коллекциями, но не все коллекции — списки
### 63. Ваше понимание IDictionary<TKey,TValue>

- Это **обобщённый интерфейс** для коллекций вида "ключ → значение".
    
- Определяет базовые операции:
    
    - доступ к элементам по ключу (`this[TKey key]`),
        
    - добавление (`Add`),
        
    - удаление (`Remove`),
        
    - проверка наличия (`ContainsKey`),
        
    - свойство `Keys` и `Values` для получения всех ключей и значений.
        
- Реализуется такими коллекциями, как `Dictionary<TKey,TValue>`, `SortedDictionary<TKey,TValue>`, `ConcurrentDictionary<TKey,TValue>` и др.
    
- Гарантирует, что ключи уникальны, а значения могут повторяться.
### 64. В чем разница между IDictionary.Add(key, value) и IDictionary[key] = value

- `Add(key, value)`
    
    - Добавляет новую пару ключ-значение.
        
    - Если ключ уже существует → выбрасывается исключение `ArgumentException`.
        
- `dictionary[key] = value`
    
    - Если ключ **не существует** → создаётся новая пара.
        
    - Если ключ **существует** → значение перезаписывается (обновляется).
        
    - Исключение не выбрасывается при повторном ключе.
### 65. Тип элемента в foreach для Dictionary

Дан код:
```csharp
var dictionary = new Dictionary<string, string>();
…..
foreach(var item in dictionary){}
```

Какой тип у item?

- Каждый элемент словаря — это структура `KeyValuePair<TKey, TValue>`.
    
- У неё есть свойства:
    
    - `Key` — ключ,
        
    - `Value` — значение.
## Обработка исключений

### 66. Опишите процесс перехвата исключительных ситуаций

При выполнении кода в блоке `try`, при возникновении исключения CLR ищет соответствующий `catch`,  
способный обработать тип исключения (или его базовый класс).

После обработки выполняется блок `finally` (если он есть).
### 67. Можно ли написать несколько блоков catch для одного оператора try?

Да ✅  
Можно — от более конкретных к более общим типам исключений.
### 68. Можно ли выполнить несколько блоков catch для одного оператора try?

Нет ❌  
Выполняется **только первый подходящий** `catch`.
### 69. Есть ли отличие в catch(Exception e){throw e;} и catch(Exception e){throw;}?

|Вариант|Поведение|
|---|---|
|`throw e;`|**Сбрасывает стек вызовов** (теряется оригинальный stack trace)|
|`throw;`|**Повторно выбрасывает исключение без потери контекста** ✅|
### 70. Выполнение блока finally

**Вопрос 1:** Будет ли выполнен блок finally, если не было сгенерировано исключение?

Дан код:
```csharp
try { 
    DoWork(); 
    return 1; 
} 
catch(Exception ex) { 
    Log(ex); 
    return 2; 
} 
finally { 
    return 3; 
}
```

**Вопрос 2:** Какое значение будет возвращено в случае возникновения исключительной ситуации в методе DoWork?

**Вопрос 3:** Какое значение будет возвращено в случае если метод DoWork отработает без ошибок?


## Методы и параметры

### 71. Ваше понимание перегрузки методов

**Перегрузка (Overloading)** — это определение **нескольких методов с одинаковым именем**,  
но **разными параметрами** (типом, количеством, порядком).
### 72. Ваше понимание рекурсии

**Рекурсия** — метод вызывает **сам себя**.  
Важно наличие **условия выхода**, иначе произойдёт переполнение стека.
### 73. Ваше понимание модификаторов ref и out
```csharp
void Change(ref int a) => a++;
void Init(out int a) => a = 10;
```

| Модификатор | Назначение                                                       |
| ----------- | ---------------------------------------------------------------- |
| `ref`       | Передаёт параметр **по ссылке**, требует инициализацию до вызова |
| `out`       | Передаёт **по ссылке**, но требует присвоения **внутри метода**  |
### 74. Ваше понимание ключевого слова params

Позволяет передавать **переменное количество аргументов** в метод.  
Фактически — массив.

📘 Пример:

```csharp
void Print(params string[] values) 
{     
	foreach (var v in values)         
		Console.WriteLine(v); 
}  

Print("A", "B", "C");
```

## Практическое задание

### 75. Работа с деревом узлов

Дан код:
```csharp
public class Node { 
    public Node Parent {get;set;} 
    public string Name {get;set;} 
    public IEnumerable<Node> Children {get;set;} 
}
```

У корневого узла Parent всегда равен null.

Предоставлен корневой узел дерева, содержащий несколько уровней:
```csharp
Node root = ReadXml();
```

**Задания:**
1. Напишите Extension-метод, возвращающий Уровень текущего узла относительно корня
2. Напишите метод рекурсивного обхода дерева, выводящий на консоль узлы в формате Уровень-Наименование


---

# SQL

## Операторы SQL

### 76. Операторы DDL

**DDL (Data Definition Language)** — язык **определения данных**.  
Используется для создания и изменения структуры объектов базы данных (таблиц, индексов и т.д.).

📘 Основные операторы:

- `CREATE` — создание объектов (таблиц, схем, индексов);
    
- `ALTER` — изменение структуры;
    
- `DROP` — удаление объектов;
    
- `TRUNCATE` — очистка таблицы без удаления структуры.
### 77. Операторы DML

**DML (Data Manipulation Language)** — язык **манипуляции данными**,  
предназначен для работы с содержимым таблиц.

📘 Основные операторы:

- `SELECT` — выбор данных;
    
- `INSERT` — вставка;
    
- `UPDATE` — обновление;
    
- `DELETE` — удаление.
### 78. Операторы защиты и управления данными

**DCL (Data Control Language)** — язык **управления доступом**.  
**TCL (Transaction Control Language)** — язык **управления транзакциями**.

📘 **DCL (Data Control Language):**

- `GRANT` — предоставить права;
    
- `REVOKE` — отозвать права.
    

📘 **TCL (Transaction Control Language)**

- `BEGIN TRANSACTION` / `START TRANSACTION` — начало;
    
- `COMMIT` — зафиксировать;
    
- `ROLLBACK` — откатить.
## Запросы и соединения

### 79. Виды JOIN

![[Pasted image 20251110141810.png]]
![[Pasted image 20251110141828.png]]

![[Pasted image 20251110143244.png]]
`JOIN` — объединяет данные из нескольких таблиц по условию связи.

| Вид          | Описание                                           |
| ------------ | -------------------------------------------------- |
| `INNER JOIN` | Только совпадающие записи                          |
| `LEFT JOIN`  | Все записи из левой таблицы + совпадения из правой |
| `RIGHT JOIN` | Все записи из правой таблицы + совпадения из левой |
| `FULL JOIN`  | Все записи из обеих таблиц                         |
| `CROSS JOIN` | Декартово произведение — все комбинации строк      |
| `SELF JOIN`  | Объединение таблицы с самой собой                  |
### 80. Что такое HAVING и чем отличается от GROUP BY

- `GROUP BY` — группирует строки по значениям столбцов.
    
- `HAVING` — фильтрует **уже сгруппированные** данные (аналог `WHERE`, но для групп).
## Оптимизация и производительность

### 81. Что такое индекс и какие виды индексов есть в БД продукта?

**Индекс** — структура данных, ускоряющая поиск строк в таблице (аналог оглавления в книге).
ПРОВЕРИТЬ КАКИЕ ЕСТЬ В ПРОДУКТЕ

| Вид индекса                      | Описание                                                                                                    | Когда использовать                                                       |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| **Clustered (кластерный)**       | Физически сортирует строки таблицы по ключу индекса. Таблица может иметь только **один** кластерный индекс. | Когда часто ищем по диапазону значений или сортируем данные.             |
| **Non-clustered (некластерный)** | Хранит отдельную структуру с ключами и указателями на строки таблицы. Может быть несколько.                 | Для ускорения поиска по конкретным столбцам.                             |
| **Unique (уникальный)**          | Гарантирует уникальность значений в столбце.                                                                | Для столбцов с уникальными значениями (например, Email, номер паспорта). |
| **Composite (составной)**        | Индекс по нескольким столбцам сразу.                                                                        | Когда часто ищем по комбинации полей (например, `Фамилия + Имя`).        |
| **Full-text (полнотекстовый)**   | Индекс для поиска по текстовым данным (слова, фразы).                                                       | Для поиска по большим текстам, статьям, описаниям.                       |
| **Spatial (пространственный)**   | Индекс для геоданных (координаты, полигоны).                                                                | В ГИС-системах, картах.                                                  |
| **Hash (хэш-индекс)**            | Использует хэш-функцию для быстрого поиска по точному совпадению.                                           | В PostgreSQL, Redis, когда нужен быстрый поиск по ключу.                 |
| **Filtered (фильтрованный)**     | Индекс только для части строк, удовлетворяющих условию.                                                     | Когда нужно индексировать только "активные" записи.                      |
### 82. Как посмотреть список выполняющихся запросов в БД продукта?

pg_stat_activity
### 83. Что такое план запроса и как его читать?
**План запроса (EXPLAIN)** — показывает, как PostgreSQL будет выполнять SQL-запрос.

**Как читать:**

- `Seq Scan` — последовательное чтение таблицы (медленно);
    
- `Index Scan` / `Index Seek` — использование индекса (быстрее);
    
- `Nested Loop`, `Hash Join`, `Merge Join` — способы объединения;
    
- `Cost` — оценка затрат (чем меньше, тем лучше);
    
- `Actual time` — реальное время выполнения.
## Транзакции и пагинация

### 84. Что такое транзакция?
**Транзакция** — это набор SQL-операций, выполняемых как одно целое (атомарно).
Либо все изменения фиксируются, либо все откатываются.
📘 **ACID-свойства транзакции:**

- **A** — Atomicity (атомарность);
    
- **C** — Consistency (согласованность);
    
- **I** — Isolation (изоляция);
    
- **D** — Durability (надёжность).
### 85. Как реализовать постраничный просмотр средствами SQL?
Используется комбинация `LIMIT` и `OFFSET`.

---

## Прогресс изучения

- [ ] .NET Framework (1-8)
- [ ] Типы данных (9-14)
- [ ] Строки (15-19)
- [ ] Модификаторы (20-35)
- [ ] Делегаты и события (36-40)
- [ ] ООП (41-50)
- [ ] Константы и приведение типов (51-53)
- [ ] Generics (54-57)
- [ ] Коллекции (58-65)
- [ ] Исключения (66-70)
- [ ] Методы (71-75)
- [ ] SQL (76-85)